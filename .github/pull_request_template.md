## What changes, and why

<!-- One or two sentences. Link the issue if there is one. -->

## Agent behaviour

- [ ] This changes a prompt, an agent manifest or a permission profile

If a permission profile was loosened, say which permission and why:

## Documentation

- [ ] `docs/` is up to date, or not affected

## How it was verified

<!--
Which checks you ran: `node --test`, `node scripts/check-syntax.mjs`,
`node scripts/release.mjs --verify`.

If this pull request touches `actions/agent/action.yml`: the self-review here
runs `actions/agent@main`, the action as it is on the default branch, not as
this pull request changes it. Say how you tested the changed action - a fork, or
a scratch repository pointing at this branch.
-->
